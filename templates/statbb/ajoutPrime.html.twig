<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#ajouterPrime">
    Mettre une Prime
</button>

<div class="modal fade" id="ajouterPrime" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">Mettre une Prime</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <div class="modal-body">
                {{ form_start(form, {'action': path('ajoutPrimeForm')}) }}
                {{ form_errors(form) }}
                {{ form_row(form.players) }}
                {{ form_row(form.montant) }}
            </div>
            <div class="modal-footer">
                {{ form_widget(form.submit) }}{{ form_widget(form.cancel) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

