{% extends 'statbb/base.html.twig' %}

{% block title %}Defis{% endblock %}

{% block body %}
<div class="m-2">
    <div class="card">
    <h1>Defis</h1>
        <a href="{{ path('Admin') }}">Retour</a>
    <table class="table" id="Admin" entity="Defis">
        <thead>
            <tr>
                <th>Id</th>
                <th>Equipe Origine</th>
                <th>Equipe Défiée</th>
                <th>Defi Realise</th>
                <th>Date Defi</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for defi in defis %}
            <tr>
                <td>{{ defi.id }}</td>
                <td>{{ defi.equipeOrigine.name }}</td>
                <td>{{ defi.equipeDefiee.name }}</td>
                <td><a href="#" data-type="select" data-pk="{{ defi.Id }}" data-name="DefieRealise" data-source='{"true": "Oui", "false":"Non"}'  id="admin_defis_realise_{{ loop.index }}"> {{ defi.defieRealise ? 'Oui' : 'Non' }}</a></td>
                <td>{{ defi.dateDefi ? defi.dateDefi|date('d/m/Y') : '' }}</td>
                <td>
                    <a href="{{ path('defis_edit', {'id': defi.id}) }}">editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('defis_new') }}">Ajouter un défi</a>
    </div>
</div>
{% endblock %}
