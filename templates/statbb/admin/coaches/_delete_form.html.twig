<form method="post" action="{{ path('coaches_delete', {'coachId': coach.coachId}) }}" onsubmit="return confirm('Etes vous sur ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.coachId) }}">
    <button class="btn">Effacer</button>
</form>
